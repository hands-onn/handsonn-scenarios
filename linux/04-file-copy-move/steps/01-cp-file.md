# Copy a single file

`cp` copies a file from one path to another. The syntax is `cp SRC DST`.

Look inside `/work/src` — there's a file called `one.txt`. Copy it into
`/work/dst/`:

```bash
ls /work/src
cp /work/src/one.txt /work/dst/one.txt
```

When you've done it, click **Check**.
